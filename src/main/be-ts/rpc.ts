import {getBook, saveSelections, listAvailableBooks, getSelections} from './data'

const rpcMethods = {
    getBook,
    saveSelections,
    listAvailableBooks,
    getSelections,
}

export {rpcMethods}